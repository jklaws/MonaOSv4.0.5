# Copy this to `/secrets.py` on the badge (or just run the Setup app — the OS
# also self-creates an empty secrets.py on first boot). Keep your real
# secrets.py out of version control (.gitignore already excludes it).

WIFI_SSID = ""          # your 2.4GHz Wi-Fi network name
WIFI_PASSWORD = ""      # Wi-Fi password
GITHUB_USERNAME = ""    # your GitHub handle (drives the per-user "badge" repo)
GITHUB_TOKEN = ""       # OPTIONAL: a fine-grained token, only for write features
